# Project Documentation Rules (Non-Obvious Only)

- Components are strictly separated: server, clients (Angular/React), shared service.
- Nix flakes are the mandatory way to build/deploy correctly.
- `scopone-rx-service` contains both library code and integration tests.
